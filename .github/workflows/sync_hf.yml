name: Sync to Hugging Face Space

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to Hugging Face Space
        run: |
          git remote add hf "https://anwxb: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@huggingface.co/spaces/anwxb/xiaobodanmu_api"
          git push --force hf HEAD:main
